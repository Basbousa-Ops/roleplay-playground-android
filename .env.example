# GEMINI_API_KEY: also synced via Supabase cloud, so you only enter it once.
# AI Studio automatically injects this at runtime from user secrets (web only).
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: web OAuth redirect (web only, Android uses a deep link instead).
APP_URL="MY_APP_URL"

# Supabase Backend Configuration (MUST match your web app for sync to work)
VITE_SUPABASE_URL=https://cwylubxrbwcjfneaxlvh.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_GQLQTNwqT8HG4vKWUkdGZg_-cdWJE6O

# Android deep link (do not change unless you also change capacitor.config.ts
# + scripts/cap-after-sync.cjs + Supabase redirect URLs):
# com.roleplay.playground://auth-callback
